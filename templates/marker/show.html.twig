{% extends 'base.html.twig' %}

{% block title %}Marker{% endblock %}

{% block body %}
    <h1>Marker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marker.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ marker.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ marker.address }}</td>
            </tr>
            <tr>
                <th>Lat</th>
                <td>{{ marker.lat }}</td>
            </tr>
            <tr>
                <th>Lng</th>
                <td>{{ marker.lng }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ marker.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marker_index') }}">back to list</a>

    <a href="{{ path('marker_edit', {'id': marker.id}) }}">edit</a>

    {{ include('marker/_delete_form.html.twig') }}
{% endblock %}
