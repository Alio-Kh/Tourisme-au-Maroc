{% extends 'base.html.twig' %}

{% block title %}Edit Resto{% endblock %}

{% block body %}
    <h1>Edit Resto</h1>

    {{ include('resto/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('resto_index') }}">back to list</a>

    {{ include('resto/_delete_form.html.twig') }}
{% endblock %}
